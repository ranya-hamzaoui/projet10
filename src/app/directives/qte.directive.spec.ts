import { QteDirective } from './qte.directive';

describe('QteDirective', () => {
  it('should create an instance', () => {
    const directive = new QteDirective();
    expect(directive).toBeTruthy();
  });
});
